<!--
Create an array of objects productsData.js and render a <Product/> component
with the data for each product in the list. Make sure to use the array's map()
to create these components. Don't forget about key prop to avoid the warning.
